const inquirer = require("inquirer")
const conTable = require('console.table');
const db = require('../connection/config')

const addEmployee = () => {
console.log('in employee helper')
};


const updateEmployee = () => {
  console.log('in employee helper')
};

const deleteEmployee = () => {
  console.log('in employee helper')
};

const viewUnsortedEmp = () => {
  console.log('in employee helper')

};

const viewEmpByDept = () => {
  console.log('in employee helper')
};

const viewEmpByMgr = () => {
  console.log('in employee helper')
};

const viewAggregateOfSalaries = () => {
  console.log('in employee helper')
};

module.exports = {addEmployee, updateEmployee, deleteEmployee, viewUnsortedEmp, viewEmpByDept, viewEmpByMgr, viewAggregateOfSalaries}
